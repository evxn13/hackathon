import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Eligence.ai - Trouvez les aides pour votre entreprise',
  description: 'Découvrez automatiquement les subventions, accompagnements et financements adaptés à votre entreprise avec notre assistant intelligent.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="fr">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
